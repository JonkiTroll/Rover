# Rover
Implementation of a rc tank. Based on rctestflight's design.

# Requirements

pyttsx3 python library for RoverTTS.py. For linux, libespeak1 needs to be installed manually (sudo apt install libespeak1).
OpenCv2 for Video_server.py. 
